# Reserved Card IR output

This directory is reserved for future per-set Card IR files. The emitter is not
implemented; `npm run build-ir` fails explicitly. Generated JSON will be ignored.
See [the proposed contract](../../../docs/data_schemas.md).
