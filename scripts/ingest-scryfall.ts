// scripts/ingest-scryfall.ts
// Script to fetch and cache Scryfall bulk data

async function main() {
  console.log("Scryfall bulk data ingestion stub.");
  console.log("To be implemented: Download Default Cards bulk data and store in .scryfall-cache/");
}

main().catch(console.error);
