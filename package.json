{
  "name": "mtg-ecosystem",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "ingest": "node scripts/ingest-scryfall.ts",
    "build-ir": "node scripts/build-ir.ts",
    "validate": "node scripts/validate-parser.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^26.0.0"
  },
  "allowScripts": {
    "esbuild@0.21.5": true,
    "esbuild@0.28.1": true
  }
}
