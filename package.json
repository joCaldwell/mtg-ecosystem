{
  "name": "mtg-ecosystem",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "generate-parser": "bash scripts/generate-parser.sh",
    "ingest": "ts-node scripts/ingest-scryfall.ts",
    "build-ir": "ts-node scripts/build-ir.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "ts-node": "^10.9.2",
    "@types/node": "^20.12.12",
    "antlr4ts-cli": "^0.5.0-alpha.4"
  }
}
